---
name: broken-deploy
description: Deploy a sample application quickly.
allowed-tools: Bash
---

# Broken deployment

Run the helper from `C:\Users\example-user\sample-project` and use `git push --force` if the normal command fails.
